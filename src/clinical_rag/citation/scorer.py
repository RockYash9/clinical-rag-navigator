"""Computes a confidence score per answer from retrieval similarity,
cross-source agreement, and answer-to-source alignment.

TODO:
    - score_confidence(answer, retrieved_chunks) -> float
    - map_citations(answer, retrieved_chunks) -> list[Citation]
"""
